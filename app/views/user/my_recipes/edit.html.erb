<%= render 'layouts/header' %>

<div class="container">
	<div class="row">
		<%= form_for @recipe, url: my_recipe_path(@recipe), method: :patch do |f| %>
		<%= render 'layouts/recipe_form', recipe: @recipe, f: f %>
		<% end %>
			</div>
	</div>
</div>