<%= render 'layouts/admin_header' %>
<div class="container">
	<div class="row">
		<div class="" style="max-width: 380px; margin: 30px auto 15px auto; text-align: center;">
		<h3>管理者プロフィール</h3>
			<table class="table">
				<tr>
					<th>名前</th>
					<td><%= @admin.name %></td>
					<td> <%= link_to "編集", edit_admin_administrator_path(current_user.id) %></td>
				</tr>
				<tr>
					<th>メールアドレス</th>
					<td><%= @admin.email %></td>
					<td> <%= link_to "編集", edit_admin_administrator_path(current_user.id) %></td>
				</tr>
			</table>
		</div>
	</div>
</div>
<%= render 'layouts/admin_footer' %>